<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nbastats">

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_nba"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewStatsList" />
        <activity android:name=".NewStats" />
        <activity android:name=".DisplayStats" />
        <activity android:name=".Games" />
        <activity android:name=".Teams" />
    </application>

</manifest>
